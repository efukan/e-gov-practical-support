# 変更履歴 (Changelog)

本ファイルは、本拡張機能の主要な変更点を記録するものです。
バージョン番号は [セマンティック バージョニング](https://semver.org/lang/ja/) に従います。

---

## [2.1.0] - 2026-09-12

被引用法令一覧の各条文リンクにマウスを乗せると、該当する条文本文がフライアウト表示されるプレビュー機能を追加しました。

### 🆕 新機能 (New Features)

- **被引用条文のフライアウトプレビュー機能を追加。**
  - 被引用一覧ポップアップ内の条文リンクにマウスを乗せると、該当条文の本文をe-Gov内部APIからオンデマンドで取得して横（フライアウト）にプレビュー表示します。
  - プレビュー本文内の引用箇所（親条文番号）を自動でハイライト表示（漢数字・全角数字・半角数字・枝番・千番台の表記揺れに対応）し、どの文脈で引用されているかが即座に把握できます。
  - 取得した法令リビジョン情報および条文DOMはセッション内で二重キャッシュされ、2回目以降は待ち時間なしで瞬時に表示されます。
  - プレビューポップアップは画面端で自動的に左右反転・クランプ配置され、マウスを直接乗せて内部をスクロールして続きを読むことができます。
  - **公式XML API（`law_file/xml`）への自動フォールバック対応**: 内部APIがHTML形式を返す法令やJSON未対応法令でも、公式XML APIから条文構造を直接取得・解析して美麗なプレビュー表示を実現。
  - **In-Flight重複通信排除（Deduplication）**: 一覧オープン時の先行プリフェッチとユーザーの初回ホバーが重なっても、通信を1本に統合して即座にプレビューを表示できるよう安定化。
- **条文中の他法令リンクホバープレビュー機能を追加。**
  - 条文本文中に出てくる他の法令の条文へのリンク（例: 「民法第七百九条」「地方自治法第十条」など）にマウスを乗せた際も、別タブを開くことなくその場でポップアッププレビューを表示できるよう拡張しました。
  - 同一法令内の条文リンク（内部リンク）は従来の高速なローカルDOM即時表示を維持しつつ、他法令への外部リンクは被引用エンジンと連携してオンデマンド取得・キャッシュ表示します。
  - プレビュー内の漢数字も横書き変換設定と自動同期します。
- **ホバー遅延時間（showDelay）の最適化による読書体験の向上。**
  - 条文読書中にマウスカーソルが文字やリンクの上を通過した際、ポップアップが不用意に開いて視界を遮らないよう、表示遅延を従来の 120ms から 300ms へ調整しました。
  - じっくり読みたい時にカーソルを留めることでスッと心地よく開き、ツールチップへ移動する非表示遅延（hideDelay）も 240ms に拡張して操作性を向上させました。
- **条文ジャンプ機能の精度向上・スクロール挙動の自然化。**
  - 参照プレビュー内の「ジャンプ」ボタンにおいて、項（Paragraph）や号（Item）へのリンクの場合に親条文ではなく目的の項・号自身へピンポイントに着地・ハイライトされるよう改善しました。
  - CSS の `scroll-margin-top` を導入し、ブラウザ標準の滑らかなスクロール機構を活用することで、高速レンダリング（`content-visibility: auto`）有効時でもレイアウト崩れや位置飛びを起こさず、固定ヘッダー下に自然な余白を持って着地できるようにしました。
  - **長距離ジャンプにおける着地ズレの解消**: 長大な法令で遠く離れた条文へジャンプする際、移動中に未レンダリングの条文が展開されてページの高さが伸縮し、着地位置がずれてしまう現象を解消しました。目標位置が遠い場合は先行して目的条文の近傍を実寸展開し、スクロール完了時（`scrollend` 検知時）にも目標条文と固定ヘッダーの間隔を自動測定してピタッと位置を合わせる精密到着補正を実装しました。
  - 画面上部の「条文ジャンプ検索」を強化し、本則だけでなく附則条文（例: 「附則1」）や枝番号（例: 「67-15-2」「67条の15の2」）、ID形式が特殊な法令のテキスト探索フォールバックに対応しました。
  - ツールチップ内アクションボタンの表記揺れID解決を共通化し、アンダースコア形式等の法令でも確実に該当条文へ移動できるようにしました。

### ⚡ 高速化・パフォーマンス最適化 (Performance)

ページ全体の動作やスクロールのもたつき、マウス移動時の負荷を徹底的に削減しました。

- **被引用ボタン挿入時の `MutationObserver` カスケード再帰発火を完全抑止。**
  - ボタン一括挿入時（数十〜数百条文）に一時的に Observer を切断し、追加ボタンごとに重い同期走査（薄字化・定義語・横書き変換）が繰り返し呼び出されていた問題を根本解消。
  - `MutationObserver` 内に拡張機能由来の自作UI要素（`.egov-ext-*`, `.egov-definition-*`）を早期除外するフィルタを追加し、自作DOMの変更による再帰的な走査・レイアウト計算を100%防止しました。
- **ツールチップの遅延評価（Lazy Content Resolution）を導入。**
  - マウスがリンクや定義語の上をすっと通過しただけの段階では、DOMクローンや横書き正規表現置換、他法令APIフェッチ通信を一切開始しないよう改善。ホバー静止（300ms経過）して「実際に表示が確定した瞬間」にのみ生成することで、カーソル移動時のCPU・ネットワーク消費をゼロ化しました。
- **ScrollSpy（目次ハイライト）の Layout Thrashing（強制同期レイアウト）解消。**
  - `IntersectionObserver` 内での直接的な同期測定を `requestAnimationFrame` で次フレームに集約・スロットリング。
  - 同一ターゲット要素にとどまっている間の不要なクラス付け替えや `scrollIntoView()` をスキップし、スクロール時のカクつきを大幅に解消しました。
- **他法令リンク解析の WeakMap メモ化。**
  - 条文リンクの法令名・条番号の抽出結果をメモリキャッシュし、同一リンクへのホバー時に親要素テキスト探索や正規表現が重複実行されないよう最適化しました。
- **e-Gov画面のGPU合成レイヤー分離と `content-visibility` 粒度最適化（表示を崩さない軽量化）。**
  - サイドバー（目次）や固定UI要素に `contain: layout` を適用し、本文スクロール時の巻き込み再描画（Repaint）を完全に遮断。
  - `content-visibility: auto` の対象を項単位（細かすぎる境界）から条文・別表・前文ブロック単位へ再編し、ブラウザの交差判定負荷を 1/5〜1/10 に大幅低減。
  - ツールチップに `will-change: transform, opacity` を付与し、ホバー時のGPUアクセラレーションを強化。

### 🛡️ 堅牢性・実機操作シミュレーション対応 (Robustness & Bug Fixes)

- **ポップアップ上でのマウス移動時・ボタン押しアプローチ時の意図せぬ非表示バグの解消。**
  - 参照条文ポップアップや被引用プレビュー内の「ジャンプ」「開く」ボタンを押しに行く際、ポップアップ上にマウスがあるにもかかわらず消えてしまう問題を解消しました。
  - プレビュー本文に含まれる条文リンク（`<a>`）や定義語ハイライトの上をマウスが通過した際、イベント監視がそれを新たなホバー対象として誤認し、ツールチップが自壊・強制終了していた問題を、ポップアップ内要素の早期除外ガードにより完全に防止しました。
  - アンカーとポップアップの間の物理的な隙間（8px）を埋める透明なホバーブリッジ（`::before`）を設置し、マウスが隙間を通過する間もホバーが途切れないようにしました。
  - ポップアップへ移動する非表示猶予時間（`hideDelay`）を従来の 240ms から 380ms に拡張し、落ち着いたマウス移動でも余裕をもってボタンへ到達できるようにしました。
  - ポップアップ内部でマウスが動いている間（`mousemove`）は非表示タイマーを継続クリアするアクティブガードと、親ツールチップ・子プレビュー間の相互保護を配備しました。
- **手振れ・子要素間移動によるタイマー破壊・開かない問題の根本改修。**
  - リンクや被引用ボタン内部の子要素（アイコンやラベルテキスト等）間をマウスが微小移動した際に発生する `mouseout` において、移動先がアンカー内部であるか（`anchor.contains(relatedTarget)`）を判定するガードを追加。
  - `pendingAnchor`（待機中アンカー）管理を新設し、アンカー内部でのマウス移動で表示タイマーがリセット・キャンセルされるのを防ぎ、確実に開くよう改善。
- **高速通信（300ms未満）完了時の「読み込み中...」フリーズ完全根絶。**
  - 非同期通信がホバー遅延（300ms）より高速に完了した際、表示状態（`visible`）を待たずに即座に本文プレビューDOMを描画するよう修正。
  - ターゲット照合に `pendingAnchor` を含め、通信完了時にプレビューがローディング画面のまま一生止まる不具合を根絶。
  - `parseSentenceToDOM` を拡張し、APIレスポンスの文字列・オブジェクト構造のブレにも柔軟に対応。
- **実ブラウザ操作シミュレーション型テストスイートへの刷新。**
  - 机上の関数テストだけでなく、アンカー内部での連続手振れ移動、高速・低速非同期通信タイムライン、キャッシュ即時切り替え、自作UIによる `MutationObserver` 自己連鎖防止テストなど、実機環境で発生しうるエッジケースを網羅するテスト体系を構築。
- **枝番号（の）多用法令におけるハイライト用語展開の根本修正。**
  - `extractHighlightTerms` において、「第六十七条の十五の二」等深い枝番を含む条番号から漢数字・全角数字・半角数字の表記揺れ（「第六十七条の十五の二」「第６７条の１５の２」「第67条の15の2」）をパーツごとに正確に分解・再構成するよう刷新。複合文字列全体を数値変換関数に渡して無関係な数値に化けていた問題を完全解消。
- **ポップアップ内条文における「引用」ボタンの非表示・サニタイズ対応。**
  - 同一法令内の参照条文ポップアップや定義語プレビューなど、ポップアップ内に表示される条文の見出し横に「引用」ボタンが混入して表示されてしまう不具合を修正しました。
  - プレビュー生成時のDOMクローン処理（`buildPreviewContent`）および共通成形処理（`ext.formatInlinePreview`）においてボタン要素を確実に除去するとともに、CSS（`style.css`）側でもツールチップ・プレビューコンテナ内部の引用ボタンを非表示（`display: none !important;`）にする二重の対策を実施しました。
- **ポップアップ内アクションボタン（別タブで開く / この条文へジャンプ）の新設と動作の最適化。**
  - **他法令プレビュー（別タブで開く）**: 本文中の他法令リンクホバー時、および被引用一覧内の条文リンクホバー時の両方において、ポップアップ右上のヘッダーに「開く」ボタンを配置しました。プレビューDOMのクローン時にもイベントが消失しないよう属性ベースのグローバルイベントデリゲーションを導入し、クリック時に確実に別タブで対象の法令・条文が開くよう堅牢化しました。
  - **本法令内プレビュー（この条文へジャンプ）**: 同一法令内の参照条文ポップアップ右上のヘッダーに「ジャンプ」ボタンを配置しました。クリックするとポップアップを閉じて該当条文の場所へ移動し、ハイライト表示します。空アンカー参照時の可視見出しへの解決、固定ヘッダーおよび上部余白（約28px）を考慮した自然な着地位置の計算、滑らかなイージング（easeInOutCubic）、到着時の画面跳ね（カクつき）の完全解消を行い、自然で心地よいスクロールを実現しました。


- **旧字体「條」を含む文語体・歴史的法令への横書き変換対応。**
  - `horizontal.js` の正規表現パターンに旧字体「條」を追加し、刑法施行法や手形法などのカタカナ文語体法令でも条文番号が綺麗に算用数字に変換されるよう拡張。
- **多様な法令パターン（10パターン）を網羅する新テストスイート（`test_law_patterns.js`）の配備。**
  - 単文法令（失火責任法）、文語体法令（刑法施行法）、枝番号法令（租税特別措置法）、重層構造（建築基準法）、別表（関税定率法）、附則構造（民法附則）、極小法令（元号法）、長大階層法令（会社法）、下位法令（施行令/規則）、HTML形式（XMLフォールバック）の全10パターンを自動検証するテストスイートを配備し、`npm test` に統合。
- **機能間の相互作用・順序依存性・重複実行耐性の抜本的強化（`test_interactions.js`）。**
  - **親子要素の復元競合解消**: `saveOriginalHTML` の退避対象セレクタから包含関係を持つ親コンテナ（Paragraph等）を除去し、互いに独立した末端の行・文ブロック単位（ParagraphSentence, ParagraphNum, ItemTitle等）に統一。子要素の復元結果が後から親要素によって上書き破壊される不具合を根本解消。
  - **自作UI要素の共通除外ガード**: `ext.SELF_UI_SELECTOR` を新設し、`collectLeafBlocks` や `horizontal.js`、`dim.js`、`definition.js` の全走査からツールチップ・被引用ボタン・ジャンプ検索等の自作DOMを完全に除外。自作UIがどの順序でDOMに追加されても誤走査・破壊されない安全性を確立。
  - **オリジナルHTML保存のサニタイズ**: 退避対象要素内に被引用ボタン等の自作UIが含まれていた場合、クローンから自作UIを除去した純粋な元HTMLを保存する安全弁を追加（ゾンビボタンの復活を完全根絶）。
  - **括弧薄字化と定義語ハイライトのスタイル競合解決**: カッコ薄字化と定義語が入れ子になった場合でも、定義語のスタイル（深緑色＋点線下線）が常に最優先されるようセレクタ特異性を強化。
  - **包括的相互作用テストスイートの配備**: 機能の実行順序逆転、3回連続多重実行（冪等性）、設定トグル復元サイクル（OFF→ON）、複合ネスト（カッコ内リンク・定義語）、動的ノード追加時の同期・非同期競合を網羅する専用テスト `test_interactions.js` を配備。全自動テスト（4本立て）で100% PASSを確認。
- **拡張機能総合健全性（Integrity）の確立と印刷・キーボードアクセシビリティの強化。**
  - **印刷スタイル（`@media print`）の完全遮断・原本コントラスト保護**: 印刷時・PDF出力時に条文横の「被引用」ボタン、モーダル、オーバーレイ、トースト等の自作UI全要素が確実に非表示となるよう指定を強化。さらに括弧薄字化や定義語の下線が法令原本同様にクリアに出力されるよう印刷コントラスト保護ルールを追加。
  - **条文ジャンプ検索のキーボード操作性強化**: 検索入力欄内で `Escape` キーを押した際に入力内容をクリアしてフォーカスを外す（blur）ハンドラを追加。
  - **多言語対応の完全化**: `manifest.json` の `action.default_title` を `__MSG_extName__` に統一し、英語OS環境でのローカライズを完備。
  - **総合健全性テストスイート（`test_extension_integrity.js`）の新設**: i18n多言語整合性、設定UI・ストレージ同期、キーボードA11y、印刷CSS、配布ZIPパッケージ整合性を網羅する16項目のテストスイートを配備し、全自動テスト（5本立て）100% PASS を達成。

### 📄 ドキュメント・開示情報の更新

- Chrome Web Storeストア掲載用の「短い説明」（107文字）および `package.json` の説明文を更新。
- `PRIVACY.md` において、被引用機能による `laws.e-gov.go.jp/internal-api/` へのPOST通信を明記（公式サイト `https://efukan.jp/privacy` §3 と整合）。
- `options.html` の説明文を実際のボタン表記「引用」およびプレビュー機能の案内に更新。

---

## [2.0.0] - 2026-08-03

内部構造の全面的な見直しと、ホバーポップアップの刷新を行った大型アップデートです。
**ユーザー向けの機能の追加・削除はありません**（既存9機能はすべてそのまま利用できます）。

### ⚡ 高速化 (Performance)

巨大な法令（会社法など）での体感速度を改善するため、DOM 走査の無駄を削減しました。

- **Shadow DOM の全要素走査を初回1回のみに限定。** 以前は DOM が変化するたびに全要素を列挙して Shadow Root を探しており、拡張機能自身の書き換えが引き金となって走査が繰り返し発生していました。
- **子孫要素数のカウントを上限で打ち切るよう変更。** 同期処理の可否を判定する際に、10万要素あっても全件を列挙してから件数を比較していました。あわせて、同一ノードに対する二重計算も解消しています。
- **祖先要素の探索を1回に集約。** 法令本文コンテナの判定で `closest()` を4回連続で呼んでいた箇所を、カンマ区切りの単一セレクタに置き換えました。
- **末端ブロックの判定方法を変更。** 全子孫を列挙してから件数を見るのではなく、最初の1件で打ち切る方式に変更しました（薄字化・定義語・横書き変換の3機能すべて）。
- **`mouseover` リスナーを3本から1本に統合。** マウスを動かすたびに同種の祖先探索が3回走っていました。Shadow Root が存在しないページでは、イベント経路の走査自体を省略します。
- **設定切り替え時の DOM 復元を3回から1回に削減。** 各機能の無効化処理がそれぞれ全体復元を呼び出していました。
- **定義語の誤検出フィルタを最適化。** 正規表現オブジェクトを1マッチごとに5個生成していた箇所を、モジュール読み込み時の1回に変更しました。
- **ツールチップ表示時の強制同期レイアウトを解消。** 「配置 → 測定 → 表示」を1パスにまとめました。

### ✨ ホバーポップアップの刷新 (Hover Popups)

参照条文プレビュー・定義語ホバー辞書・被引用一覧の3種を、共通基盤（`js/tooltip.js`）に統合しました。

- **ポップアップ内をスクロール・クリックできるようになりました。** 従来は `pointer-events: none` が指定されていたため、長い条文の続きを読むことができませんでした。
- **画面端で見切れなくなりました。** 下端では上向きに反転し、左右はビューポート内に収まるよう自動調整します。
- **表示ディレイ (120ms) を追加。** マウスが通過しただけでポップアップがちらつくことがなくなりました。
- **非表示ディレイ (180ms) を追加。** ポップアップ本体へマウスを移動する余裕ができました。
- **デザインを刷新。** 角丸12px、多層シャドウ、アンカーを指す矢印、フェード＋わずかなスライドで表示されます。3種のポップアップで見た目と挙動が統一されました。

### ♿ アクセシビリティ (Accessibility)

- 定義語を `Tab` キーで辿れるようになり、フォーカスするだけで定義が表示されます。
- `Escape` キーでポップアップを閉じられます。
- ポップアップに `role="tooltip"` と `aria-describedby` を付与しました。
- OS の「視差効果を減らす」設定 (`prefers-reduced-motion`) を尊重し、アニメーションを抑制します。

### 🧹 内部構造の整理 (Refactoring)

- **`js/settings.js` を新設。** 設定のデフォルト値が3ファイルに重複定義されていたのを解消し、保存・読み込み・設定UIの共通ロジックを集約しました。`popup.js` と `options.js` はほぼ同一の内容でしたが、差分のみを持つ薄いファイルになりました。
- **`js/tooltip.js` を新設。** ポップアップの生成・配置・表示制御を集約しました。
- **重複ロジックを `js/utils.js` に集約。** 法令ID抽出（3実装）、本文コンテナ取得（9箇所）、末端ブロック抽出（3実装）、除外セレクタ一覧（2実装）、全角・半角変換をそれぞれ1つにまとめました。
- **死にコードを削除。** 呼び出し元が存在しない正規化キュー、到達不能なフォールバック処理、実行されない条件分岐を取り除きました。
- **デバッグ出力を整理。** 本番コードに残っていた `console.log` を、開発時のみ有効になるログ関数に置き換えました。

### 🔧 その他 (Misc)

- **外部フォント (Google Fonts) の読み込みを廃止。** 設定画面がオフラインでも崩れなくなり、外部への通信もなくなりました。OS 標準のフォントを使用します。
- **定義語の下線を `text-decoration` ベースに変更。** 行の高さが広がらず、複数行にまたがる語でも下線が正しく折り返します。
- `package.json` のライセンス表記の誤り (ISC) を修正しました（正しくは MIT）。
- **自動テストを追加。** `npm test` で、content script 一式の読み込みからポップアップ表示・復元までの動作確認と、ポップアップ配置ロジックの検証が実行できます。

---

## [1.2.1] - 2026-07

- 国際化構造 (`_locales`) の追加。

## [1.2.0] - 2026-07

- Chrome ウェブストア公開用のバージョン調整。

## [1.1.2] - 2026-07-05

- 印刷時に拡張機能のUI（ステータスバッジ・検索窓など）を非表示にするよう修正。

## [1.1.0] - 2026-07-05

- 被引用（引用元）表示機能を追加。
- Shadow DOM に対応。
- ポップアップ挙動の堅牢化（グローバルイベントデリゲーションの導入）。
- 同一法令内でのタブ切り替えに伴う定義語消失バグを修正。

## [1.0.0] - 2026-06-23

- 初回リリース。横書き表記変換、括弧書きの薄字化、定義語ホバー辞書、目次の自動追従、条文ジャンプ検索を統合。
