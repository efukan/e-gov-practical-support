# 変更履歴 (Changelog)

本ファイルは、本拡張機能の主要な変更点を記録するものです。
バージョン番号は [セマンティック バージョニング](https://semver.org/lang/ja/) に従います。

---

## [2.2.0] - 2026-09-19

設定画面およびポップアップのMaterial 3準拠ダークモード対応、ポップアップ余白・プロポーションの上品バランス化、非条文ページでの完全機能停止、および実機法令における表示成形の徹底最適化を実施しました。

### 🆕 新機能 (New Features)

- **設定画面およびポップアップのダークモード対応（Material 3準拠）。**
  - 詳細設定画面（`options.html`）およびツールバーポップアップ（`popup.html`）が、OSやブラウザのダークテーマ（`prefers-color-scheme: dark`）に完全自動追従するようになりました。
  - Google Material 3 Design ガイドラインに基づいたダークサーフェス（背景 `#191c20`、カード `#22262b`、アクセント `#a8c7fa`、高コントラスト白テキスト `#e2e2e6`）を採用。
  - スイッチ（ON/OFF、チェックマーク反転）、バージョンバッジ、定義語カラーパレット、プレビュー枠等のすべてのUIコンポーネントが暗色環境下で洗練された視認性と統一感を保持します。
  - `<meta name="color-scheme" content="light dark">` および `:root:not([data-theme="light"])` を採用し、システム追従と手動検証の双方に対応。

### 🎨 デザイン・レイアウト最適化 (Design & Layout Improvements)

- **ポップアップの余白・プロポーション・行間の上品バランス化。**
  - e-Gov公式CSS（`.laws div { padding: 0 }`）によるパディング消失バグをセレクタ詳細度強化（`!important` 付き）により根本解決。
  - 矢印三角（▲）の白背景がヘッダー文字の上に被さるスタッキングコンテキストの課題を解決し、テキストを確実に前面化（`z-index: 1`）するとともに安全クリアランス（約3.5px）を確保して文字欠けを完全防止。
  - ユーザー指定に基づくカスタムプロポーション（行間 `1.72`、外周パディング 上8/左右12/下10px、ヘッダー下空き計8px、最大幅450px）を適用し、間延び感のない引き締まった上質な読書体験を実現。
- **条文第1項のインライン表示（第◯条直後全角1文字空け）。**
  - 参照条文ポップアップにおいて、第1項を法令原本の慣例どおり「第◯条　本文...」と条番号直後に全角1文字空けてインライン配置し、第2項以降を独立行として表示。
- **同一法令内参照ポップアップヘッダーにおける参照箇所（条・項・号）の明示。**
  - ヘッダーに `参照条文（第◯条第◯項）` や `参照条文（第◯条から第◯条まで）` のようにピンポイントな参照先を明示。相対参照（前条・同条・同項等）でもリンク先IDから完全な条項号パスを自動補完。
- **定義語ポップアップヘッダーの算用数字・枝番号・号番号カッコ除去正規化。**
  - ヘッダーの出典条文（例: `第２条の２第１項第３号`）を法令横書き標準の全角アラビア数字へ自動変換し、号番号の余計なカッコ（`第(3)号`）を排除して「第３号」「第３号の２」として美麗に出力。

### 🛡️ 安定性・堅牢化 (Stability & Bug Fixes)

- **条文ページ以外（検索結果 `/result` やトップページ等）での機能完全停止。**
  - 検索結果一覧ページやポータルトップページにおいて、ホバーリスナーのバインドを抑止し、右上のステータスバッジや別タブ機能の割り込みを完全除外。SPA遷移時も安全に初期化・クリーンアップ。
- **法令番号の括弧（元号〜号）を括弧書き薄字化・虹色カッコの対象外とする改善。**
  - 他法令参照時の法令番号（例: `特定非営利活動促進法（平成１０年法律第７号）第２条`）の括弧が薄字化されてしまう問題を解消。
  - 末尾に句点「。」がつかず、公的な法令番号パターン（元号＋年＋種別＋号、枝番号、人事院規則等の特殊様式）に合致する括弧を自動検出し、通常の地の文（黒文字）として維持。
  - 一方で、体言止め注記（例: `（第五号に掲げるもの）`）や通常の注記文章（例: `（……を除く。）`、`（以下「……」という。）`）は従来通り確実に薄字化・虹色カッコを適用。
  - テキストノードが `<a>` タグ等で分割されている複雑なDOM構造下でも誤判定やレイアウト崩れなく動作。
- **実機Vue DOM由来の空テキストノード混在時における二重空白（2文字スペース）の完全解消。**
  - 定義語ポップアップや他法令プレビュー成形時、空テキストノードを完全パージし、クロス要素境界での連続空白圧縮ロジックを導入して厳密な全角1文字スペース区切りを保証。

---

## [2.1.0] - 2026-09-14

被引用法令一覧の各条文リンクにマウスを乗せると、該当する条文本文がフライアウト表示されるプレビュー機能を追加しました。また、拡張機能の名称を「ひもとき」シリーズと統一し、公式サービス名との重複を避けた「**e-Gov法令ひもとき**」（英語名: e-Gov Law Himotoki）へ変更しました。

### 🏷️ 名称・ブランド統一 (Brand & Identity)

- **拡張機能の正式名称を「e-Gov法令ひもとき」（英語名: e-Gov Law Himotoki）へ変更・統一。**
  - 「ひもとき」シリーズとしてのブランド一貫性を確立するとともに、公式ポータル「e-Gov法令検索」との名称バッティングを回避してサードパーティ支援ツールとしての識別性を高めるため、名称を改めました。
  - 多言語メッセージリソース（`_locales/ja`, `_locales/en`）、設定画面（`options.html`）、ポップアップ（`popup.html`）、法令ページ右上のステータスバッジ（`js/utils.js`）、各種ドキュメント（`README.md`, `PRIVACY.md`, `CHROMEWEBSTORE.md` 等）にわたる全表記を統一しました。
- **拡張機能アイコンを「法令ひもとき」印影デザイン（e-Govネイビー・白色）へ刷新。**
  - 本家「法令ひもとき」（himotoki.efukan.jp）の印影アイコンと同じ「-5度傾けた『法』」の印章デザインを採用しました。
  - 背景をe-Govのブランドネイビー（#1042a4）、丸枠および文字を白色（#ffffff）に統一し、円形シルエット（外側透過）と十分な外周青帯（約39px）を確保することで、ライト・ダーク双方のブラウザツールバー環境で高い視認性とシリーズとしての一体感を実現しました。
- **Chrome Web Store用プロモーションバナー・アセットの全面リニューアル。**
  - 本家「法令ひもとき」のミニマルな美しさに合わせ、ストア用バナー（小タイル 440×280、マーキー 1400×560）およびnote用カバー（1280×670）を刷新。
  - 「タイトル＋サブ見出し＋シャンパンゴールド水平線＋2行キャッチコピー（『括弧を薄く、定義語を引き／参照・引用条文を、その場に』）」の端正なタイポグラフィと垂直センタリングを確立しました。
  - バナー一括ビルドコマンド `npm run generate-promos` および配布用ZIP生成コマンド `npm run package` を新設しました。

### 🆕 新機能 (New Features)

- **接続詞の色分け（又は・若しくは・並びに・及び）機能を追加。**
  - 本家「法令ひもとき」（himotoki.efukan.jp）のデザインに準拠し、条文内の重要接続詞を自動検知して大・小の階層関係に合わせて色・太字・下線で視覚化する新機能を追加しました。
  - **並列（青系）**: 大グループ＝`並びに`（濃い青・太字・太下線）／小グループ＝`及び`（青色・細下線）
  - **選択（赤系）**: 大グループ＝`又は`（濃い赤・太字・太下線）／小グループ＝`若しくは`（赤色・細下線）
  - 括弧書きの薄字化（dim）とも自然に共存し、長大・複雑な条文でも論理構造や主語・述語の係り受けが一目で把握できます。
- **定義語の着色カラーカスタマイズ機能（既読リンク紫との重複を防ぐ緑系をデフォルト化）を追加。**
  - 定義語の強調表示色として、ブラウザの既読リンク（訪問済みリンクの紫色）との混同を防ぐため、上品で視認性の高いティール深緑（`#00695c`、点線下線）をデフォルトとしました。
  - 詳細設定画面（`options.html`）にカラーパレットUIを新設し、プリセットの5色スウォッチ（緑・紫・青・琥珀・ローズ）またはカスタムカラーピッカーから好きな色を自由に選択可能にしました（本家「法令ひもとき」標準の紫色 `#6a1b9a` にもワンクリックで変更可能）。
  - CSSカスタムプロパティ（`--egov-def-color`）および `chrome.storage.sync` を活用し、色変更時はDOM再走査なしで即座に法令本文やプレビューに反映されます。
- **被引用条文のフライアウトプレビュー機能を追加。**
  - 被引用一覧ポップアップ内の条文リンクにマウスを乗せると、該当条文の本文をe-Gov内部APIからオンデマンドで取得して横（フライアウト）にプレビュー表示します。
  - プレビュー本文内の引用箇所（親条文番号）を自動でハイライト表示（漢数字・全角数字・半角数字・枝番・千番台の表記揺れに対応）し、どの文脈で引用されているかが即座に把握できます。
  - 取得した法令リビジョン情報および条文DOMはセッション内で二重キャッシュされ、2回目以降は待ち時間なしで瞬時に表示されます。
  - プレビューポップアップは画面端で自動的に左右反転・クランプ配置され、マウスを直接乗せて内部をスクロールして続きを読むことができます。
  - **公式XML API（`law_file/xml`）への自動フォールバック対応**: 内部APIがHTML形式を返す法令やJSON未対応法令でも、公式XML APIから条文構造を直接取得・解析して美麗なプレビュー表示を実現。
  - **In-Flight重複通信排除（Deduplication）**: 一覧オープン時の先行プリフェッチとユーザーの初回ホバーが重なっても、通信を1本に統合して即座にプレビューを表示できるよう安定化。
- **条文中の他法令リンクホバープレビュー機能を追加。**
  - 条文本文中に出てくる他の法令の条文へのリンク（例: 「民法第七百九条」「地方自治法第十条」など）にマウスを乗せた際も、別タブを開くことなくその場でポップアッププレビューを表示できるよう拡張しました。
  - 同一法令内の条文リンク（内部リンク）は従来の高速なローカルDOM即時表示を維持しつつ、他法令への外部リンクは被引用エンジンと連携してオンデマンド取得・キャッシュ表示します。
  - プレビュー内の漢数字も横書き変換設定と自動同期します。
- **ホバー遅延時間（showDelay）の最適化による読書体験の向上。**
  - 条文読書中にマウスカーソルが文字やリンクの上を通過した際、ポップアップが不用意に開いて視界を遮らないよう、表示遅延を従来の 120ms から 300ms へ調整しました。
  - じっくり読みたい時にカーソルを留めることでスッと心地よく開き、ツールチップへ移動する非表示遅延（hideDelay）も 240ms に拡張して操作性を向上させました。
- **条文ジャンプ機能の精度向上・スクロール挙動の自然化。**
  - 参照プレビュー内の「ジャンプ」ボタンにおいて、項（Paragraph）や号（Item）へのリンクの場合に親条文ではなく目的の項・号自身へピンポイントに着地・ハイライトされるよう改善しました。
  - CSS の `scroll-margin-top` を導入し、ブラウザ標準の滑らかなスクロール機構を活用することで、高速レンダリング（`content-visibility: auto`）有効時でもレイアウト崩れや位置飛びを起こさず、固定ヘッダー下に自然な余白を持って着地できるようにしました。
  - **長距離ジャンプにおける着地ズレの解消**: 長大な法令で遠く離れた条文へジャンプする際、移動中に未レンダリングの条文が展開されてページの高さが伸縮し、着地位置がずれてしまう現象を解消しました。目標位置が遠い場合は先行して目的条文の近傍を実寸展開し、スクロール完了時（`scrollend` 検知時）にも目標条文と固定ヘッダーの間隔を自動測定してピタッと位置を合わせる精密到着補正を実装しました。
  - 画面上部の「条文ジャンプ検索」を強化し、本則だけでなく附則条文（例: 「附則1」）や枝番号（例: 「67-15-2」「67条の15の2」）、ID形式が特殊な法令のテキスト探索フォールバックに対応しました。
  - ツールチップ内アクションボタンの表記揺れID解決を共通化し、アンダースコア形式等の法令でも確実に該当条文へ移動できるようにしました。
- **同一法令内の参照ポップアップにおける参照箇所（条・項・号）のヘッダー明示。**
  - 同一法令内の条文リンク（内部リンク）をホバーした際、ポップアップの1行目（ヘッダー）に「参照条文（第１３１条第１項第４号）」のようにピンポイントな条・項・号の参照先を括弧書きで明示するようにしました。
  - 「前条」「次条」「同条」「同項」「第十一号」などの相対参照表記であっても、リンク先のハッシュIDから親条文を特定し「参照条文（第１５３条第１項第１１号）」のように完全な参照パスを自動補完します。
  - 「第二十七条から第二十九条まで」のように範囲を持つ指定では、文脈に沿った範囲表記（「参照条文（第２７条から第２９条まで）」）を尊重して表示します。
  - 附則（例: `参照条文（附則第３条）`）、別表（例: `参照条文（別表第１）`）、枝番号（例: `第１項の２`、`第４号の２`）にも完全対応し、法令の横書き標準である全角アラビア数字表記に自動正規化されます。
- **定義語ポップアップヘッダーにおける定義先条項（第◯条の◯）の算用数字変換・枝番号対応・号番号の正規化。**
  - 定義語ポップアップの1行目（ヘッダー）に表示される定義先条項（例: 「（定義）第二条」）について、横書き表示（算用数字変換）が有効な際に「（定義）第２条」と全角算用数字（アラビア数字）に正しく変換されるようにしました。
  - 「第二条の二」などの枝番条文においても枝番号が切り捨てられることなく「第２条の２」と正確に抽出・変換されるよう条番号抽出正規表現を拡充しました。
  - **号番号のカッコ除去と「第◯号」正規化**: 文化財保護法などの号に定義語がある場合、横書き変換後の号見出し「(3)」からカッコが混入して「第(3)号」と表示されていた問題を解消し、法令標準の美しい全角算用数字「第３号」（枝番号時は「第３号の２」）として正確に正規化・統一表示されるように改善しました。
  - ポップアップ本文（定義文）に対しても横書き変換を適用し、定義文中の条文引用も確実に算用数字表記に統一されます。
- **ポップアップ（定義語・参照条文プレビュー）における三角余白過大・条文同化被りの解消。**
  - ホバーポップアップ上端の三角矢印（▲）直下からヘッダーテキストまでの間に存在していた過大な上部余白（約18px）を、上部パディング半減（`12px` → `6px`）およびヘッダーマージン最適化（`8px` → `6px`）によりスリム化・直結化しました。
  - 外枠境界線のコントラスト向上（`rgba(16, 24, 40, 0.08)` → `0.15`）および全方向アンビエントシャドウ（`0 1px 3px rgba(16, 24, 40, 0.10)`）を新設。条文用紙（白地）と同化して「三角の直下の条文が修正テープのように白く塗りつぶされて消えた」ように見えていた現象を完全に解消し、背後テキストの上に洗練されたカードが美しく浮遊する視覚的階層感を確立しました。
- **ポップアップヘッダー文字の矢印三角（::after）による欠け防止（z-index前面化・余白・サイズ最適化）。**
  - ポップアップ上部の吹き出し矢印（45度回転の正方形）の内側領域（下向き三角 ▼ の白背景）がヘッダーテキスト（例: 「(文化財の定義)」の「財」の文字上端）に被さることで、文字の上端が斜め三角形に欠けて見えていた不具合を解消しました。
  - スクロールコンテナ（`.egov-ext-tip-scroll`）に `position: relative; z-index: 1` を指定し、矢印擬似要素（`z-index: 0`）よりも確実にテキストを前面へ配置。さらに上部パディング（`9px`）と矢印サイズ（`9px`、`top: -5px`）を微調整し、矢印の先端下端とヘッダー文字上端の間に安全なクリアランス（約4.5px）を確保することで、定義語・参照条文ポップアップの双方で文字の欠け・重なりを完全に防止しました。
- **非条文ページ（検索結果 /result やトップページ等）における機能完全停止・クリーンアップ対応。**
  - e-Gov法令検索の検索結果一覧ページ（`https://laws.e-gov.go.jp/result`）やトップページなど、法令条文ページ以外の場所において、拡張機能の各種機能（ポップアップ、ステータスバッジ、条文ジャンプ検索、リンククリックの横取り等）が一切動作しないよう判定を厳格化しました。
  - **検索結果でのポップアップ誤爆防止**: 検索結果一覧に並ぶ法令リンク（`a[href*="/law/"]`）にマウスが乗っても、プレビューAPI通信やポップアップ表示を100%遮断するように改善しました。
  - **不要UIの完全非表示**: 非条文ページでは右上のステータスバッジや条文ジャンプ検索ボックスを生成せず、画面をすっきりとした元の状態に保ちます（条文ページ内でのみ「e-Gov法令ひもとき: 稼働中」を表示）。
  - **リンククリックの横取り防止**: 「別タブで開く」機能が条文ページ内でのみ発動するよう制限し、検索結果一覧での通常の画面遷移が阻害されないようにしました。
  - **SPA（Vue Router）画面遷移への追従**: トップ ⇄ 検索結果 ⇄ 条文ページ 間のブラウザバックや画面遷移を自動検知し、非条文ページへ戻った際は自作UIやツールチップを即時クリーンアップし、条文ページへ入った時だけ自動起動するよう連動させました。



### ⚡ 高速化・パフォーマンス最適化 (Performance)

ページ全体の動作やスクロールのもたつき、マウス移動時の負荷を徹底的に削減しました。

- **被引用ボタン挿入時の `MutationObserver` カスケード再帰発火を完全抑止。**
  - ボタン一括挿入時（数十〜数百条文）に一時的に Observer を切断し、追加ボタンごとに重い同期走査（薄字化・定義語・横書き変換）が繰り返し呼び出されていた問題を根本解消。
  - `MutationObserver` 内に拡張機能由来の自作UI要素（`.egov-ext-*`, `.egov-definition-*`）を早期除外するフィルタを追加し、自作DOMの変更による再帰的な走査・レイアウト計算を100%防止しました。
- **ツールチップの遅延評価（Lazy Content Resolution）を導入。**
  - マウスがリンクや定義語の上をすっと通過しただけの段階では、DOMクローンや横書き正規表現置換、他法令APIフェッチ通信を一切開始しないよう改善。ホバー静止（300ms経過）して「実際に表示が確定した瞬間」にのみ生成することで、カーソル移動時のCPU・ネットワーク消費をゼロ化しました。
- **ScrollSpy（目次ハイライト）の Layout Thrashing（強制同期レイアウト）解消。**
  - `IntersectionObserver` 内での直接的な同期測定を `requestAnimationFrame` で次フレームに集約・スロットリング。
  - 同一ターゲット要素にとどまっている間の不要なクラス付け替えや `scrollIntoView()` をスキップし、スクロール時のカクつきを大幅に解消しました。
- **他法令リンク解析の WeakMap メモ化。**
  - 条文リンクの法令名・条番号の抽出結果をメモリキャッシュし、同一リンクへのホバー時に親要素テキスト探索や正規表現が重複実行されないよう最適化しました。
- **e-Gov画面のGPU合成レイヤー分離と `content-visibility` 粒度最適化（表示を崩さない軽量化）。**
  - サイドバー（目次）や固定UI要素に `contain: layout` を適用し、本文スクロール時の巻き込み再描画（Repaint）を完全に遮断。
  - `content-visibility: auto` の対象を項単位（細かすぎる境界）から条文・別表・前文ブロック単位へ再編し、ブラウザの交差判定負荷を 1/5〜1/10 に大幅低減。
  - ツールチップに `will-change: transform, opacity` を付与し、ホバー時のGPUアクセラレーションを強化。

### 🛡️ 堅牢性・実機操作シミュレーション対応 (Robustness & Bug Fixes)

- **ポップアップ外周余白およびヘッダー下余白の引き締め（コンパクト・スマート化）。**
  - e-Gov公式CSS（`.laws div { padding: 0 }`）によるパディング消失バグ解消後、実機上でパディングが正常反映されたことに伴い、外周余白やヘッダー下の境界線周辺の空きが広すぎる点を、ユーザーのご指定に基づき理想的なプロポーションにカスタム調整しました。
  - **外周パディング・矢印クリアランス（案3ベース）**: `padding: 8px 12px 10px !important;`（上8px、左右12px、下10px）に調整。上部8pxにより矢印（▲）侵入（約4.5px）に対して約3.5pxの安全クリアランスを確保し文字欠けを防ぎつつ、コンパクトで引き締まった外観を実現しました。
  - **ヘッダー下余白（案2ベース）**: `.laws .egov-ext-tip-header` の下マージンを `4px`、下パディングを `4px`（合計8px、従前の15pxから約5割削減）に引き締め、ヘッダー線と本文の間の間隔をバランス良く整理しました。
  - **行間と最大幅**: 行間を `line-height: 1.72 !important;` に設定して法律文書の可読性を確保しつつ、最大幅を本文ポップアップ `450px`、フライアウトプレビュー `435px` に整え、省スペース性と読みやすさを両立しました。
- **e-Gov公式CSS「.laws div { padding: 0 }」によるポップアップ余白消失バグの特定と根本解決。**
  - 実機e-Gov法令検索の画面上において、指定したはずのポップアップ外周パディングがすべて `0px` にリセットされ、ヘッダー・本文・ジャンプボタンが外枠境界線に激突・密着していた現象の根本原因を特定・完全解消しました。
  - **原因特定**: e-Gov公式のスタイルシート（`common.css`）に存在する全称的リセット `body.laws div { margin: 0; padding: 0; }`（詳細度: 0, 1, 1）が、拡張機能の `.egov-ext-tip`（詳細度: 0, 1, 0）に対して詳細度で勝ってしまい、指定していたパディングが完全に握りつぶされて消滅していました。
  - **根本解決**: セレクタ詳細度を `.laws .egov-ext-tip, .egov-ext-tip`（詳細度: 0, 2, 0）へ強化し、`padding: 14px 20px 16px !important;`、`box-sizing: border-box !important;`、`max-width: var(--tip-max-w) !important;` を付与。さらに内側の `.laws .egov-ext-tip-scroll` や `.laws .egov-ext-tip-header` にも `!important` を徹底することで、e-Gov公式CSSのいかなる打ち消しにも影響されない強固なレイアウト保護を確立しました。
  - **効果**: 空家法をはじめとするすべての法令において、実機画面上のポップアップ四方（上下14〜16px、左右20px）に豊かなホワイトスペースが確実に適用され、窮屈感が完全に払拭された上品で読みやすいデザインを実現しました。
- **ポップアップ周囲の余白・最大幅・行間の最適化（ゆとりのある上質なデザイン対応）。**
  - 定義語ポップアップ、参照条文ポップアップ、被引用法令一覧等において、文字がポップアップ外枠の端っこギリギリまで表示されて狭苦しい印象を与えていたデザインを刷新しました。
  - 外周パディングを `padding: 14px 20px 16px;`（上下 14/16px、左右 20px）に拡張し、カードの周囲全体に適度なホワイトスペース（余白）を確保しました。
  - 最大幅を `--tip-max-w: 490px;`（フライアウトプレビューは `470px`）に広げ、左右余白の拡張後も1行あたりの文字収容力と折り返しの美しさを両立させました。
  - 日本語法律文書の漢字密度に合わせ、`line-height: 1.72` に調整し、ヘッダー・段落間のマージンやスクロールバーとの干渉余白（`padding-right: 2px`）、被引用リンクの内側パディング（`7px 11px`）も適正化し、窮屈感のない読みやすいUIを実現しました。
- **検索結果ページ（/result）やトップページ等の非条文ページにおける機能完全停止・クリーンアップ対応。**
  - 検索結果一覧やトップページなど条文本文が存在しない画面において、他法令プレビューのホバー発火や右上ステータスバッジの表示、別タブ遷移の横取りを完全に抑止し、条文ページ以外では自作UIを即座に破棄・クリーンアップする安全ガードを整備しました（SPA画面遷移にも追従）。
  - 参照条文ポップアップや被引用プレビュー内の「ジャンプ」「開く」ボタンを押しに行く際、ポップアップ上にマウスがあるにもかかわらず消えてしまう問題を解消しました。
  - プレビュー本文に含まれる条文リンク（`<a>`）や定義語ハイライトの上をマウスが通過した際、イベント監視がそれを新たなホバー対象として誤認し、ツールチップが自壊・強制終了していた問題を、ポップアップ内要素の早期除外ガードにより完全に防止しました。
  - 親ツールチップと子ツールチップ（フライアウトプレビュー）の相互非表示解除における循環呼び出し（無限再帰）を遮断し、被引用ボタンや一覧内リンクのホバー時に確実にポップアップとプレビューが表示されるよう安定化しました。
  - ポップアップ内部でマウスが動いている間（`mousemove`）は非表示タイマーを継続クリアするアクティブガードを配備しました。
- **被引用法令一覧および条文プレビューの非表示不具合解消・スクロール連動閉鎖の実装。**
  - 被引用一覧から条文プレビューを開いた後、マウスを画面外に出した際に被引用一覧が画面に残り続けてしまう不具合を解消しました。子ツールチップ（プレビュー）からマウスが離脱した際、移動先が親ツールチップ自身やそのアンカーでなければ、親ツールチップも連動して非表示予約を行うよう親子間の非表示伝播を整備しました。
  - ページ本体がスクロールされた際、開いているツールチップを即時に閉じるスクロール検知閉鎖を実装しました。ツールチップ内部（`.egov-ext-tip-scroll`）のスクロール操作は適切に除外・保護されるため、長文条文や一覧の閲覧スクロールを損なうことなく、ページスクロール時にツールチップが画面に取り残される現象を完全に防止しました。
  - ツールチップ外部の画面余白をクリックした際にも即時にツールチップを閉じる画面外クリック閉鎖（Dismiss）を追加しました。
  - 非表示猶予時間（`hideDelay`）を 300ms に最適化し、ボタン押し移動の操作性を保ちつつ、マウスを外に出した際のキレの良さ（素早い非表示）を両立しました。
- **ツールチップ重なり順序（z-index）の修正（ステータスバッジ背面潜り込みの解消）。**
  - 画面上部付近の条文や定義語にホバーした際、ツールチップが右上の「e-Gov実務サポート: 稼働中（現：e-Gov法令ひもとき: 稼働中）」ステータスバッジや条文ジャンプ検索ボックスの下に潜り込んでしまう重なり順の不具合を解消しました。
  - ツールチップ（`.egov-ext-tip`, `.egov-ext-tip--preview`）の `z-index` を最前面レイヤーに設定し、ステータスバッジコンテナよりも常に前面に表示されるようレイヤー階層を適正化しました。
- **手振れ・子要素間移動によるタイマー破壊・開かない問題の根本改修。**
  - リンクや被引用ボタン内部の子要素（アイコンやラベルテキスト等）間をマウスが微小移動した際に発生する `mouseout` において、移動先がアンカー内部であるか（`anchor.contains(relatedTarget)`）を判定するガードを追加。
  - `pendingAnchor`（待機中アンカー）管理を新設し、アンカー内部でのマウス移動で表示タイマーがリセット・キャンセルされるのを防ぎ、確実に開くよう改善。
- **高速通信（300ms未満）完了時の「読み込み中...」フリーズ完全根絶。**
  - 非同期通信がホバー遅延（300ms）より高速に完了した際、表示状態（`visible`）を待たずに即座に本文プレビューDOMを描画するよう修正。
  - ターゲット照合に `pendingAnchor` を含め、通信完了時にプレビューがローディング画面のまま一生止まる不具合を根絶。
  - `parseSentenceToDOM` を拡張し、APIレスポンスの文字列・オブジェクト構造のブレにも柔軟に対応。
- **実ブラウザ操作シミュレーション型テストスイートへの刷新。**
  - 机上の関数テストだけでなく、アンカー内部での連続手振れ移動、高速・低速非同期通信タイムライン、キャッシュ即時切り替え、自作UIによる `MutationObserver` 自己連鎖防止テストなど、実機環境で発生しうるエッジケースを網羅するテスト体系を構築。
- **枝番号（の）多用法令におけるハイライト用語展開の根本修正。**
  - `extractHighlightTerms` において、「第六十七条の十五の二」等深い枝番を含む条番号から漢数字・全角数字・半角数字の表記揺れ（「第六十七条の十五の二」「第６７条の１５の２」「第67条の15の2」）をパーツごとに正確に分解・再構成するよう刷新。複合文字列全体を数値変換関数に渡して無関係な数値に化けていた問題を完全解消。
- **ポップアップ内条文における「引用」ボタンの非表示・サニタイズ対応。**
  - 同一法令内の参照条文ポップアップや定義語プレビューなど、ポップアップ内に表示される条文の見出し横に「引用」ボタンが混入して表示されてしまう不具合を修正しました。
  - プレビュー生成時のDOMクローン処理（`buildPreviewContent`）および共通成形処理（`ext.formatInlinePreview`）においてボタン要素を確実に除去するとともに、CSS（`style.css`）側でもツールチップ・プレビューコンテナ内部の引用ボタンを非表示（`display: none !important;`）にする二重の対策を実施しました。
- **ポップアップ内アクションボタン（別タブで開く / この条文へジャンプ）の新設と動作の最適化。**
  - **他法令プレビュー（別タブで開く）**: 本文中の他法令リンクホバー時、および被引用一覧内の条文リンクホバー時の両方において、ポップアップ右上のヘッダーに「開く」ボタンを配置しました。プレビューDOMのクローン時にもイベントが消失しないよう属性ベースのグローバルイベントデリゲーションを導入し、クリック時に確実に別タブで対象の法令・条文が開くよう堅牢化しました。
  - **本法令内プレビュー（この条文へジャンプ）**: 同一法令内の参照条文ポップアップ右上のヘッダーに「ジャンプ」ボタンを配置しました。クリックするとポップアップを閉じて該当条文の場所へ移動し、ハイライト表示します。空アンカー参照時の可視見出しへの解決、固定ヘッダーおよび上部余白（約28px）を考慮した自然な着地位置の計算、滑らかなイージング（easeInOutCubic）、到着時の画面跳ね（カクつき）の完全解消を行い、自然で心地よいスクロールを実現しました。


- **号本文の表示不具合（Column構造対応）の解消。**
  - 建築基準法第52条第1項（容積率）など、複数列・区分構造（`ItemSentence.Column`）で構成される号において、従来の単一文構造（`Sentence`）のみを想定していたため本文が空白になり号番号（「一」「二」…）しか表示されなかった不具合を改修しました。
  - 各 `Column` 要素内のセンテンスを抽出し、列間に全角スペース（`\u3000`）を挟んで自然に連結して描画するよう拡張しました。軽量な内部JSON APIだけでなく、公式XML APIフォールバック時（`<Column>` ノード解析）にも完全対応しています。
- **号番号の算用数字(1)化対応（横書き機能連動）および二重空白（2文字スペース）の完全解消。**
  - 算用数字（横書き）機能が有効な場合、他法令プレビューおよび定義語ポップアップ内の号番号（「一」「二」…）を `(1)`, `(2)` （枝番がある場合は `(1)の2`）に自動変換して表示するよう拡張しました。
  - **建築基準法施行令等の旧様式法令における二重空白の解消**: 旧様式（`_div_ItemSentence`）法令において、原本の `span` 直後に全角スペーステキストノードが存在する場合、号番号変換関数が無条件にスペースを付与したことで `(1)　　敷地` とスペースが2文字分空いてしまっていた不具合を改修しました。原本の末尾空白有無の判定と要素境界正規化（`processItem`）を導入し、新様式（Vue）および旧様式の双方で厳密に全角1文字スペース（`(1)　敷地`）に統一しました。
- **建築基準法等における定義語ハイライト消失の解消（抽出Promise共有・完了フラグ保護・高速化）。**
  - 建築基準法のような超長大法令において、ページの初期レンダリング時や動的変更時に非同期チャンクタスクが途中で中断され、インライン略称（パターン1）の途中で抽出が打ち切られていた不具合を改修しました。
  - 未完の状態で `definitionMap.size > 0` となり以降の抽出がスキップされていたため、第2条のカラム分割定義（「建築物」「特殊建築物」「建築設備」「主要構造部」等）が一切登録されず本文でも色がついていませんでした。
  - 抽出処理のPromise共有（Deduplication）、全パターン完走を保証する `definitionExtractionCompleted` フラグ、走査セレクタ適正化（不要な子孫要素排除）、チャンクサイズの最適化（500件）を実施し、DOM更新によるタスク再入時でも抽出が安全に完走して全155語・3,509箇所の定義語が確実にハイライト表示されるよう根本改修しました。
- **ポップアップ条文レイアウトの正常化（不自然な改行・2文字スペース崩れの解消）。**
  - 定義語ポップアップや参照条文プレビューにおいて、号番号（`ItemTitle`）、定義語・各区分列（`Column`）、本文（`Sentence`）が `div`（ブロック要素）のまま描画されて縦積み3行に改行されてしまっていた不具合を改修しました。
  - 共通成形関数（`ext.formatInlinePreview`）およびCSS（`style.css`）を改修し、要素をインライン配置（`display: inline !important;`）に統一するとともに、タグ間の余計な改行テキストノードを除去し、号番号後およびカラム境界のスペースを厳密に全角1文字に正規化（e-Gov原本由来の空白との二重挿入による2文字スペース化を完全防止）することで、原本法令通りの自然で美しい1行流し込みレイアウトを再現しました。
- **定義語ポップアップにおける空テキストノード起因の二重空白（2文字スペース）の完全解消。**
  - 実機e-GovのVueコンポーネント（`LawBodyColumn`）において、コメントノード（`<!---->`）の前後に長さ0の空テキストノード（`nodeValue === ""`）が残存していた場合、TreeWalkerがこの空ノードを末尾と誤認して直前の全角スペース（`　`）の除去をスキップし、後続処理でスペースが二重挿入されてしまう問題を根本改修しました。
  - 成形処理冒頭での空テキストノードの完全パージ（`node.remove()`）、カラム末尾・先頭テキストノードの配列化と逆順/順方向ループによる実文字ベースの確実な空白除去、および要素境界をまたぐ連続空白の正規化ロジックを導入し、どのようなVue動的DOM構造でも二重空白を100%防止し、原本通りの厳密な全角1文字スペースに統一しました。
- **参照条文プレビューにおける条文第1項の第◯条直後全角1文字空けインライン配置対応。**
  - 他法令プレビューや被引用条文プレビュー、同一法令内の参照条文プレビューにおいて、条番号（「第◯条」）が独立したブロック要素として描画され、直後で改行されて第1項本文が表示されていた問題を解消しました。
  - 法令の正式な表示基準に従い、見出し（`ArticleCaption`）の直後の行で、条番号（`ArticleTitle`）に続けて全角1文字空けて第1項（第一項目）本文がインラインで始まるようレイアウトを適正化しました（第2項以降は従来通り改行された独立行として表示されます）。
  - 横書き表示機能（`horizontal`）有効時の条番号の算用数字化（「第６条」等）にも完全連動しました。
- **他法令プレビューにおけるヘッダータイトル二重化の解消および正式法令名の優先表示。**
  - 条文内のリンクが「法第５２条第１項」「令第二条」などのように略称（「法」「令」「規則」等）で書かれている場合、条番号の分割パース漏れとフォールバックの重複連結により「法第５２条第１項第５２条第１項」とタイトルが二重化してしまっていた問題を改修しました。
  - 略称付きリンクの正規表現を改善して条番号パスを正確に分離するとともに、APIから取得した正式な法令名（`content.LawTitle`、例: 「建築基準法」）をヘッダーに最優先で反映するよう改善しました。これにより、略称表記のリンクからでも正確で美しい法令名・条文番号ヘッダーが表示されます。
- **旧字体「條」を含む文語体・歴史的法令への横書き変換対応。**
  - `horizontal.js` の正規表現パターンに旧字体「條」を追加し、刑法施行法や手形法などのカタカナ文語体法令でも条文番号が綺麗に算用数字に変換されるよう拡張。
- **多様な法令パターン（10パターン）を網羅する新テストスイート（`test_law_patterns.js`）の配備。**
  - 単文法令（失火責任法）、文語体法令（刑法施行法）、枝番号法令（租税特別措置法）、重層構造（建築基準法）、別表（関税定率法）、附則構造（民法附則）、極小法令（元号法）、長大階層法令（会社法）、下位法令（施行令/規則）、HTML形式（XMLフォールバック）の全10パターンを自動検証するテストスイートを配備し、`npm test` に統合。
- **機能間の相互作用・順序依存性・重複実行耐性の抜本的強化（`test_interactions.js`）。**
  - **親子要素の復元競合解消**: `saveOriginalHTML` の退避対象セレクタから包含関係を持つ親コンテナ（Paragraph等）を除去し、互いに独立した末端の行・文ブロック単位（ParagraphSentence, ParagraphNum, ItemTitle等）に統一。子要素の復元結果が後から親要素によって上書き破壊される不具合を根本解消。
  - **自作UI要素の共通除外ガード**: `ext.SELF_UI_SELECTOR` を新設し、`collectLeafBlocks` や `horizontal.js`、`dim.js`、`definition.js` の全走査からツールチップ・被引用ボタン・ジャンプ検索等の自作DOMを完全に除外。自作UIがどの順序でDOMに追加されても誤走査・破壊されない安全性を確立。
  - **オリジナルHTML保存のサニタイズ**: 退避対象要素内に被引用ボタン等の自作UIが含まれていた場合、クローンから自作UIを除去した純粋な元HTMLを保存する安全弁を追加（ゾンビボタンの復活を完全根絶）。
  - **括弧薄字化と定義語ハイライトのスタイル競合解決**: カッコ薄字化と定義語が入れ子になった場合でも、定義語のスタイル（深緑色＋点線下線）が常に最優先されるようセレクタ特異性を強化。
  - **包括的相互作用テストスイートの配備**: 機能の実行順序逆転、3回連続多重実行（冪等性）、設定トグル復元サイクル（OFF→ON）、複合ネスト（カッコ内リンク・定義語）、動的ノード追加時の同期・非同期競合を網羅する専用テスト `test_interactions.js` を配備。全自動テスト（4本立て）で100% PASSを確認。
- **拡張機能総合健全性（Integrity）の確立と印刷・キーボードアクセシビリティの強化。**
  - **印刷スタイル（`@media print`）の完全遮断・原本コントラスト保護**: 印刷時・PDF出力時に条文横の「被引用」ボタン、モーダル、オーバーレイ、トースト等の自作UI全要素が確実に非表示となるよう指定を強化。さらに括弧薄字化や定義語の下線が法令原本同様にクリアに出力されるよう印刷コントラスト保護ルールを追加。
  - **条文ジャンプ検索のキーボード操作性強化**: 検索入力欄内で `Escape` キーを押した際に入力内容をクリアしてフォーカスを外す（blur）ハンドラを追加。
  - **多言語対応の完全化**: `manifest.json` の `action.default_title` を `__MSG_extName__` に統一し、英語OS環境でのローカライズを完備。
  - **総合健全性テストスイート（`test_extension_integrity.js`）の新設**: i18n多言語整合性、設定UI・ストレージ同期、キーボードA11y、印刷CSS、配布ZIPパッケージ整合性を網羅する16項目のテストスイートを配備し、全自動テスト（5本立て）100% PASS を達成。

### 📄 ドキュメント・開示情報の更新

- Chrome Web Storeストア掲載用の「短い説明」（107文字）および `package.json` の説明文を更新。
- `PRIVACY.md` において、被引用機能による `laws.e-gov.go.jp/internal-api/` へのPOST通信を明記（公式サイト `https://efukan.jp/privacy` §3 と整合）。
- `options.html` の説明文を実際のボタン表記「引用」およびプレビュー機能の案内に更新。
- **不要なホスト権限（elaws.e-gov.go.jp）の除外と開示文書の整合。**
  - ソースコード（`js/`, `css/`, `*.html`）内で `elaws.e-gov.go.jp` が一切参照されておらず、また同ドメインへのリクエストはHTTP 301により `laws.e-gov.go.jp` へ即座にリダイレクトされるため、利用者が滞在せず content script が動作する場面がありません。
  - 未使用のホスト権限を保持することによる審査リスクを回避するため、`manifest.json` の `host_permissions` および `content_scripts.matches` から `*://elaws.e-gov.go.jp/*` を削除し、`*://laws.e-gov.go.jp/*` のみに一本化しました。
  - あわせて、開示文書（`PRIVACY.md`、`README.md`、`CHROMEWEBSTORE.md`）の対象ドメイン記述を整理し、ストア権限申告（`storage` および `laws.e-gov.go.jp`）の理由説明をAPI通信等の実態に合わせて最新化しました。

---

## [2.0.0] - 2026-08-03

内部構造の全面的な見直しと、ホバーポップアップの刷新を行った大型アップデートです。
**ユーザー向けの機能の追加・削除はありません**（既存9機能はすべてそのまま利用できます）。

### ⚡ 高速化 (Performance)

巨大な法令（会社法など）での体感速度を改善するため、DOM 走査の無駄を削減しました。

- **Shadow DOM の全要素走査を初回1回のみに限定。** 以前は DOM が変化するたびに全要素を列挙して Shadow Root を探しており、拡張機能自身の書き換えが引き金となって走査が繰り返し発生していました。
- **子孫要素数のカウントを上限で打ち切るよう変更。** 同期処理の可否を判定する際に、10万要素あっても全件を列挙してから件数を比較していました。あわせて、同一ノードに対する二重計算も解消しています。
- **祖先要素の探索を1回に集約。** 法令本文コンテナの判定で `closest()` を4回連続で呼んでいた箇所を、カンマ区切りの単一セレクタに置き換えました。
- **末端ブロックの判定方法を変更。** 全子孫を列挙してから件数を見るのではなく、最初の1件で打ち切る方式に変更しました（薄字化・定義語・横書き変換の3機能すべて）。
- **`mouseover` リスナーを3本から1本に統合。** マウスを動かすたびに同種の祖先探索が3回走っていました。Shadow Root が存在しないページでは、イベント経路の走査自体を省略します。
- **設定切り替え時の DOM 復元を3回から1回に削減。** 各機能の無効化処理がそれぞれ全体復元を呼び出していました。
- **定義語の誤検出フィルタを最適化。** 正規表現オブジェクトを1マッチごとに5個生成していた箇所を、モジュール読み込み時の1回に変更しました。
- **ツールチップ表示時の強制同期レイアウトを解消。** 「配置 → 測定 → 表示」を1パスにまとめました。

### ✨ ホバーポップアップの刷新 (Hover Popups)

参照条文プレビュー・定義語ホバー辞書・被引用一覧の3種を、共通基盤（`js/tooltip.js`）に統合しました。

- **ポップアップ内をスクロール・クリックできるようになりました。** 従来は `pointer-events: none` が指定されていたため、長い条文の続きを読むことができませんでした。
- **画面端で見切れなくなりました。** 下端では上向きに反転し、左右はビューポート内に収まるよう自動調整します。
- **表示ディレイ (120ms) を追加。** マウスが通過しただけでポップアップがちらつくことがなくなりました。
- **非表示ディレイ (180ms) を追加。** ポップアップ本体へマウスを移動する余裕ができました。
- **デザインを刷新。** 角丸12px、多層シャドウ、アンカーを指す矢印、フェード＋わずかなスライドで表示されます。3種のポップアップで見た目と挙動が統一されました。

### ♿ アクセシビリティ (Accessibility)

- 定義語を `Tab` キーで辿れるようになり、フォーカスするだけで定義が表示されます。
- `Escape` キーでポップアップを閉じられます。
- ポップアップに `role="tooltip"` と `aria-describedby` を付与しました。
- OS の「視差効果を減らす」設定 (`prefers-reduced-motion`) を尊重し、アニメーションを抑制します。

### 🧹 内部構造の整理 (Refactoring)

- **`js/settings.js` を新設。** 設定のデフォルト値が3ファイルに重複定義されていたのを解消し、保存・読み込み・設定UIの共通ロジックを集約しました。`popup.js` と `options.js` はほぼ同一の内容でしたが、差分のみを持つ薄いファイルになりました。
- **`js/tooltip.js` を新設。** ポップアップの生成・配置・表示制御を集約しました。
- **重複ロジックを `js/utils.js` に集約。** 法令ID抽出（3実装）、本文コンテナ取得（9箇所）、末端ブロック抽出（3実装）、除外セレクタ一覧（2実装）、全角・半角変換をそれぞれ1つにまとめました。
- **死にコードを削除。** 呼び出し元が存在しない正規化キュー、到達不能なフォールバック処理、実行されない条件分岐を取り除きました。
- **デバッグ出力を整理。** 本番コードに残っていた `console.log` を、開発時のみ有効になるログ関数に置き換えました。

### 🔧 その他 (Misc)

- **外部フォント (Google Fonts) の読み込みを廃止。** 設定画面がオフラインでも崩れなくなり、外部への通信もなくなりました。OS 標準のフォントを使用します。
- **定義語の下線を `text-decoration` ベースに変更。** 行の高さが広がらず、複数行にまたがる語でも下線が正しく折り返します。
- `package.json` のライセンス表記の誤り (ISC) を修正しました（正しくは MIT）。
- **自動テストを追加。** `npm test` で、content script 一式の読み込みからポップアップ表示・復元までの動作確認と、ポップアップ配置ロジックの検証が実行できます。

---

## [1.2.1] - 2026-07

- 国際化構造 (`_locales`) の追加。

## [1.2.0] - 2026-07

- Chrome ウェブストア公開用のバージョン調整。

## [1.1.2] - 2026-07-05

- 印刷時に拡張機能のUI（ステータスバッジ・検索窓など）を非表示にするよう修正。

## [1.1.0] - 2026-07-05

- 被引用（引用元）表示機能を追加。
- Shadow DOM に対応。
- ポップアップ挙動の堅牢化（グローバルイベントデリゲーションの導入）。
- 同一法令内でのタブ切り替えに伴う定義語消失バグを修正。

## [1.0.0] - 2026-06-23

- 初回リリース。横書き表記変換、括弧書きの薄字化、定義語ホバー辞書、目次の自動追従、条文ジャンプ検索を統合。
